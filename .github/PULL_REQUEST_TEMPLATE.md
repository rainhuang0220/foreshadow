## Summary

<!-- What does this PR change, and why? -->

## Plan task

<!-- e.g. Task 1 — repo skeleton. Or "n/a". -->

## Checklist

- [ ] Tests added/updated for this change
- [ ] `uv run ruff check src tests` passes
- [ ] `uv run ruff format --check src tests` passes
- [ ] `uv run pytest` passes
- [ ] No live `api.github.com` calls in tests
- [ ] No tokens / `.env` / local DB committed
- [ ] README caveats still accurate if user-facing behavior changed
